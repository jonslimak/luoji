# OpenClaw Setup Progress

Last updated: 2026-02-17
Status: Milestone 1 complete; Anthropic-primary validated. OpenAI replay-hardening is documented for fallback/canary and currently paused (non-blocking).

## Current Snapshot

- Canonical docs in repo: `plan.md`, `task.md`, `progress.txt`.
- Installation target is this Mac mini.
- Workflow: GitHub is source of truth; milestone tags and rollback notes are required; no secrets committed.

## 2026-02-16

1. Planning and workflow baseline
   - Updated `plan.md` and `task.md` for cross-machine workflow and Milestone 0 tasks.
   - Added `progress.txt` timeline and `release-rollback-note-template.md`.
2. Milestone 0 completed
   - Verified clean repo state and remote connectivity.
   - Set up GitHub SSH auth and switched `origin` to `git@github.com:jonslimak/luoji.git`.
   - Validated push path via branch `codex/m0-baseline-docs`.
   - Created and pushed annotated rollback tag `v2026.02.16-1`.
   - Performed rollback dry run; verified tag resolves to commit `1bf67dd`.
3. Milestone 1 setup completed (local secure baseline)
   - Installed Node.js 22 via `nvm` and `openclaw` `2026.2.15` via npm (official installer path was blocked in non-interactive flow).
   - Ran non-interactive onboard with loopback gateway, token auth, and daemon install.
   - Ran doctor/repair, tightened `~/.openclaw` permissions, and created credentials directory.
4. OpenAI provider vertical slice passed
   - Configured OpenAI auth; default model set to `openai/gpt-5.1-codex`.
   - Validated gateway status, model auth, CLI chat smoke test, and dashboard launch.
   - Closed M1 in `task.md`; deferred non-blocking warning about runtime pinned to `nvm` Node path.
5. Post-M1 runtime issues and immediate recovery
   - UI ENOENT for `/Users/luo/.openclaw/workspace/MEMORY.md` resolved by creating file.
   - OpenAI Responses 400 reasoning-pairing error appeared after first assistant turn in UI sessions.
   - Initial session-index cleanup and gateway restart provided only temporary relief.
6. Replay error root cause and A/B fix validation
   - Root cause: OpenAI tool-call ID sanitization removed `|` from `call_id|fc_id`, breaking replay pairing between `reasoning` and `function_call`.
   - Evidence: reproducible multi-session failures; error occurred at replay validation (`usage.input=0`, `usage.output=0`).
   - A/B validation:
     - Before patch: session `abtest-openai-400` failed with recurring 400.
     - After patch: excluded OpenAI from `sanitizeToolCallIds`; same session path and follow-up turn succeeded.
   - Applied local hotfix to 5 installed OpenClaw dist files under npm package path; restarted gateway.
   - Caveat: local dist hotfix can be overwritten by reinstall or upgrade.

## 2026-02-17

1. Replay hardening moved to scripted workflow
   - Added `scripts/patch-openclaw-replay-compat.sh` (version-scoped patch automation; optional gateway restart).
   - Added `scripts/verify-openclaw-replay.sh` (deterministic two-turn replay gate).
   - Added `docs/replay-compat-gate.md` (release-blocking patch+verify policy).
2. Priority realignment (user-directed)
   - Anthropic became primary implementation target.
   - OpenAI replay work remains documented for fallback/canary and is not blocking primary lane.
3. Anthropic-primary implementation and validation completed
   - Model routing set to default `anthropic/claude-sonnet-4-5` and fallback `openai/gpt-5.1-codex`.
   - Initial auth gap caused fallback usage; resolved by adding Anthropic key and restarting gateway.
   - Validation passed in CLI and UI paths:
     - `anthro-primary-verify` turn 1 and turn 2 succeeded on provider `anthropic`.
     - `anthro-ui-check` succeeded; transcript shows `modelApi=anthropic-messages`, `model=claude-sonnet-4-5`.
     - No replay 400 observed during this validation window.
4. OpenAI fallback/canary lane status (paused)
   - Kept in repo: replay patch script, replay verify script, and replay gate doc.
   - Deferred: full replay-gate run on next OpenClaw upgrade cycle and deployment/upstream follow-through.
5. GitHub push checkpoint
   - Branch: `codex/m1-local-vertical-slice`.
   - Commit: `87f5dcc` (push range `57f370b..87f5dcc`).
   - Included Anthropic-primary updates, replay-hardening scripts/docs, and repo hygiene/catalog additions.
6. Feature catalog workflow added
   - Added canonical catalog: `docs/clawbot-feature-catalog.md`.
   - Added browser viewer: `docs/clawbot-feature-catalog.html`.
   - Added per-feature tracking fields (`status`, `notes`) tied to progress/task evidence.
7. Five-file context completion and cleanup
   - Created pre-change backup snapshot at `/Users/luo/.openclaw/backups/five-file-cleanup-20260217-171012`.
   - Snapshot covers workspace markdown, sessions, logs, and sqlite memory; manifest recorded 27 files with sha256.
   - Added lean startup memory policy and daily-memory scaffolding.
   - Added runbook: `docs/five-file-context-runbook.md`.
   - Updated tracking: Feature Block 10 set to Done and `F5-*` checklist added in `task.md`.
   - No deletion of existing logs, sessions, sqlite memory, or prior memory files.
8. Session support completed
   - Verified dashboard launch (`openclaw dashboard`) from this machine.
   - Restored local feature-catalog browsing via local docs server on port 8000.
   - Session-end focus set to Telegram integration planning; no Telegram config mutations were made.

## Next Actions (Priority)

1. Start Milestone 2 (remote access via Tailscale Serve):
   - run `openclaw dashboard` and send one local test message
   - install/authenticate Tailscale on this Mac mini
   - enable OpenClaw Tailscale Serve mode with gateway still loopback-bound
   - validate remote UI from a second tailnet device
2. Create Milestone 2 rollback point after soft gate pass.
3. Optional fallback lane follow-up on future OpenClaw updates:
   - `scripts/patch-openclaw-replay-compat.sh --restart-gateway`
   - `scripts/verify-openclaw-replay.sh`
   - decide whether to complete canary automation work or remove it

## Notes

- Soft gates are reviewed per milestone.
- Keep vertical-slice-first execution to avoid setup drift.
- OpenAI key shared during setup should be rotated/revoked and replaced in local runtime secrets.
