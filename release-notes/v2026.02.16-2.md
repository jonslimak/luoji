# Release and Rollback Note

Last updated: 2026-02-16

## Milestone Metadata

- Milestone: Milestone 1 - Local secure vertical slice
- Date (YYYY-MM-DD): 2026-02-16
- Tag: `v2026.02.16-2`
- Commit SHA: See tag target for `v2026.02.16-2`
- Machine: Target Mac mini (`luos-Mac-mini.local`)

## What Changed

1. Installed OpenClaw CLI (`2026.2.15`) with Node.js 22 on target machine.
2. Completed onboarding with daemon install, loopback bind, and gateway token auth.
3. Configured OpenAI as active provider with default model `openai/gpt-5.1-codex`.
4. Verified local chat flow and Control UI dashboard access.
5. Updated milestone tracking docs (`task.md`, `progress.txt`).

## What Was Verified

1. Command: `openclaw gateway status`
   - Result: LaunchAgent loaded, gateway runtime running, RPC probe OK on loopback.
2. Command: `openclaw models status`
   - Result: OpenAI auth detected; default model set to `openai/gpt-5.1-codex`.
3. Command: `openclaw agent --session-id m1-smoke --message "Reply with exactly: M1 chat OK" --json`
   - Result: Successful response payload returned expected text (`M1 chat OK`).
4. Command: `openclaw dashboard`
   - Result: Local Control UI URL opened successfully.
5. Command: `openclaw doctor --non-interactive`
   - Result: No blocking/critical issues; non-blocking Node runtime warning remains.

## Known Risks

1. Gateway service currently references Node from `nvm`; system-managed Node runtime migration is deferred to a hardening milestone.
2. Reverse proxy trusted headers warning remains expected while still in local-only loopback mode.

## Rollback Commands

1. Code rollback:
   - `git fetch --tags`
   - `git checkout v2026.02.16-1`
2. Runtime rollback:
   - Restore OpenClaw state snapshot matching milestone baseline if needed.
   - State path: `/Users/luo/.openclaw`
   - Snapshot timestamp: capture externally if snapshot tooling is used.

## Secrets Check

- [x] No secrets committed to GitHub
- [x] Tokens/keys stored only on target machine
- [x] Sensitive values redacted in notes/logs

